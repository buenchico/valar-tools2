<div class="row">
  <% ["a","b"].each do | side | %>
    <div class="col-6">
      <div class="row h1 text-right ">
        <%= @battle.send("side_#{side}") %>
      </div>
      <div>
        <%= link_to t('battles.edit.add_armies'), add_battle_armies_path(@battle, side: side), remote: true, class: 'btn btn-danger' %>
      </div>
      Tipo de movimiento
      Tipo de terreno
      Estrategia de escaramuza (sigilo/info)
      Miscelánea Sigilo
      Miscelánea Información
      Ventaja?
    </div>
  <% end %>
</div>
