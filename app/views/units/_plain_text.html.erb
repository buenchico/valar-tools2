<%= unit.name %>
<%= number_to_compact(unit.count.to_i) %>
<%= unit.type_name %>
<% unit.tags.tally.each do |tag, count| %>
  <% tag_options = @unit_tags.fetch(tag, {}) %>
  <%= tag_options.fetch('name', tag).titleize %>
  <%= (count != 1) ? "(#{count})" : "" %>
<% end %>

<% if unit.factions.present? %>
  (<%= unit.factions.pluck(:long_name).join(", ") %>)
<% end %>
