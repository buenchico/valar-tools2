<div class="modal fade" id="armyModal" tabindex="-1" role="dialog" aria-labelledby="Login" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content bg-dark">
      <div class="modal-header">
        <h5 class="modal-title w-100" id="Title"><%= army&.name %></h5>
        <div class="w-100">
          <%= link_to '', send("edit_army_path", army.id), remote: true, class: 'btn-Close bi bi-pencil-square text-white float-end' %>
        </div>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= render 'army_data', army: army %>
      </div>
    </div>
  </div>
