$('#faction_stats').html("<%= j render 'faction_stats', faction: @faction %>")
$('#army_table').html("<%= j render 'armies/armies_table', armies: @armies %>")
$('#unit_table').html("<%= j render 'units/units_table', units: @units %>")

$('.selectpicker').selectpicker();

$('.spinner').addClass('d-none');
$('.armies-load').attr('disabled', false) 

initPopovers();
$('.selectpicker').selectpicker();
$.fn.table_sorter();
$.fn.checkbox_listeners();
$.fn.inline_listeners();
$.fn.copyArmies();
$.fn.tableFilters('unit');
$.fn.tableFilters('army');
$.fn.recalculateStats();
