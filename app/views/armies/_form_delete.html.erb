<div class="modal fade" id="armyModal" tabindex="-1" role="dialog" aria-labelledby="Login" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content bg-dark text-light">
      <div class="modal-header">
        <h5 class="modal-title w-100" id="Title"><%= t('helpers.submit.delete', model: t('activerecord.models.army', count: 1) ) %></h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= form_with url: army_path(army), method: :delete, local: false do |form| %>
          <div class="alert alert-warning">
            <div>
              <strong>
                <%= render 'one_line', army: army %>
              </strong>
              será borrado, ¿estás seguro?
            </div>
            <hr class="border border-secondary opacity-75 border-2">
            <div>
              Las siguientes unidades serán liberadas:
              <% army.units.each do |unit| %>
                <div class="ms-1">
                  <%= render 'units/one_line', unit: unit %>
                </div>
              <% end %>
            </div>
          </div>
          <div class="input-group mt-3">
            <%= form.text_field :confirm, class: "form-control", placeholder: t('helpers.validate.delete') %>
          </div>

          <div class="input-group mt-3">
            <%= form.submit t('helpers.submit.delete', model: Army.model_name.human), class: 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
