$(document).on('turbolinks:load', function () {
  if ($(".armies").length !== 0) {
    $(document).on('changed.bs.select', '.units-select', function (e, clickedIndex, isSelected, previousValue) {
      const selectedValue = $(this).val();
      console.log('New value:', selectedValue);
    });

    $('.units-select').selectpicker(); // Ensure it's initialized
  }
});
